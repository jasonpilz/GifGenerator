<h1>Categories Index</h1>

<% @categories.each do |cat| %>
  <li>
    <ul><%= cat.title %> <%= link_to "Delete", admin_category_path(cat), method: :delete, id: "cat_#{cat.title}"  %></ul>
  </li>
<% end %>
